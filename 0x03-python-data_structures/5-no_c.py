#!/usr/bin/python3

def no_c(my_string):
    new_string = []
    for i in len(my_string):
        if my_string[i] == "c" or if my_string[i] == "C":
            new_string[i] = ''
        new_string[i] = my_string[i]
    return (new_string)
